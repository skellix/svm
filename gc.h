#ifndef CG_H
#define GC_H

//#include <gc/gc.h>

//#define malloc(size) GC_MALLOC_UNCOLLECTABLE(size)
//#define calloc(size) GC_MALLOC_UNCOLLECTABLE(size)
//#define realloc(origin, size) GC_REALLOC(origin, size)
//#define free(origin) GC_free(origin)

#endif
